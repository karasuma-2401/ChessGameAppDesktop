<UserControl
    x:Class="ChessUI.Views.BoardMenu.CountDownView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="600"
    d:DesignWidth="600"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Storyboard x:Key="TextPopAnim">
            <DoubleAnimation
                Storyboard.TargetName="TextScale"
                Storyboard.TargetProperty="ScaleX"
                From="0.0"
                To="1.0"
                Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

            <DoubleAnimation
                Storyboard.TargetName="TextScale"
                Storyboard.TargetProperty="ScaleY"
                From="0.0"
                To="1.0"
                Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

            <DoubleAnimation
                Storyboard.TargetName="CountText"
                Storyboard.TargetProperty="Opacity"
                From="0.0"
                To="1.0"
                Duration="0:0:0.2" />
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Border Background="{DynamicResource Brush.Glass}" />

        <TextBlock
            x:Name="CountText"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="150"
            FontWeight="Bold"
            Foreground="#FFA274"
            RenderTransformOrigin="0.5, 0.5"
            Text="">
            <TextBlock.Effect>
                <DropShadowEffect
                    BlurRadius="30"
                    Opacity="0.6"
                    ShadowDepth="5"
                    Color="Black" />
            </TextBlock.Effect>

            <TextBlock.RenderTransform>
                <ScaleTransform x:Name="TextScale" ScaleX="1" ScaleY="1" />
            </TextBlock.RenderTransform>
        </TextBlock>
    </Grid>
</UserControl>